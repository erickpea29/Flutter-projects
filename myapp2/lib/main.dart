import 'package:flutter/material.dart';
import 'Estructura.dart';

void main() {
  /*runApp - generar los elementos de la pantalla 
  MAterialApp - Widget (cualquier elemento grafico o lógico) que devuelve widgets basicos y comunes */
  return runApp(MaterialApp(home: Estructura())); //fin del runApp
}
